#!groovy
pipeline {
    agent any
    stages{
        stage('Clone'){
            steps {
                git https://github.com/AnneRey/movie-analyst-ui.git
                sh "pwd"
                sh "ls"
            }
        }
        stage('Package'){
            step{
                sh "docker build -t movie-analyst-ui:1.0"
                sh "docker images"
            }
        }
    }
}